const appConfig = {
  // Add any configuration values needed by the tests
  chainConfig: {
    tokens: [],
    chains: [],
  },
  apis: {
    registry: {
      apiUrl: "https://registry.cfce.io/api",
    },
  },
}

export default appConfig
