{
  "extends": ["//"],
  "globalEnv": [
    "POSTGRES_PRISMA_URL",
    "BLOB_READ_WRITE_TOKEN",
    "XINFIN_MINTER_SECRET",
    "XINFIN_NETWORK",
    "TURBO_ENV",
    "NEXT_PUBLIC_APP_ID",
    "NEXT_PUBLIC_APP_ENV"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["@cfce/types#build"]
    }
  }
}
