{
  "name": "@cfce/types",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "rm -rf dist && tsc",
    "copy-declarations": "mkdir -p dist/declarations && cp -r src/declarations/* dist/declarations/"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "22.5.5",
    "next-auth": "5.0.0-beta.25",
    "typescript": "latest"
  }
}
