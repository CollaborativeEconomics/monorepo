I wanted to add this to the `app-config` package, but currently nextjs doesn't support ESM in the `next.config.ts` file (why?).

So I'm just going to put it here for now.

Further reading: https://github.com/vercel/next.js/pull/68365

TODO: move it over someday