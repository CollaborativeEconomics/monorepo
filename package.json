{
  "name": "cfce-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "pnpm biome format --write /**/*.{ts,tsx,md}",
    "matchVersions": "syncpack fix-mismatches",
    "updateDependencies": "syncpack update"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.2",
    "@cfce/typescript-config": "workspace:*",
    "syncpack": "^13.0.0",
    "turbo": "2.1.3"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
